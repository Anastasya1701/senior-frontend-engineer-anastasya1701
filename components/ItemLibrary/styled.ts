import styled from 'styled-components'

export const ItemLibraryWrapper = styled.div`
    max-width: 1200px;
    padding-left: calc(12px);
    padding-right: calc(12px);
    margin: 0 auto;
`

export const ItemLibraryHeader = styled.header`
    display: flex;
    align-items: center;
    margin-bottom: 16px;
    z-index: 29;
    min-height: 60px;
    font-size: 20px;
    font-weight: bold;
`